---
description: Show current Dojo status (work ratio and line counts)
---

# Dojo Status

**Note:** The CLI is at `<plugin-dir>/dist/cli.js` where plugin-dir is the directory containing this command file.

```bash
node <plugin-dir>/dist/cli.js status
```

Report the results clearly, highlighting whether the ratio is healthy or needs attention.
